import { memo } from "react";
import type { MCPServerInfo } from "app-types/mcp";
import type { BasicUser } from "app-types/user";

// Main MCPCard component - temporarily stubbed
export const MCPCard = memo(function MCPCard(props: MCPServerInfo & { user: BasicUser }) {
  return null;
});
