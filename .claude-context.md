# Better Chatbot - 项目上下文

## 项目结构
\`\`\`
src/
├── app/              # Next.js App Router (页面 + API)
├── components/       # React 组件
│   ├── agent/       # Agent 相关组件
│   ├── layouts/     # 布局组件
│   └── ui/          # 基础 UI 组件
├── lib/             # 工具库
│   ├── auth/        # 认证
│   ├── db/          # 数据库 (Drizzle ORM)
│   └── hooks/       # 自定义 hooks
├── hooks/           # React hooks
├── types/           # TypeScript 类型定义
└── i18n/            # 国际化
\`\`\`

## 技术栈
- **框架**: Next.js 15 (App Router)
- **语言**: TypeScript
- **数据库**: PostgreSQL + Drizzle ORM
- **认证**: Better Auth
- **AI**: Vercel AI SDK
- **测试**: Playwright (E2E), Vitest (Unit)
- **样式**: Tailwind CSS + shadcn/ui

## 代码规范
- 组件命名: PascalCase (如 \`AgentList.tsx\`)
- API routes: \`app/api/[resource]/route.ts\`
- 类型定义: \`src/types/*.ts\`
- 工具函数: \`src/lib/utils/*.ts\`

## 数据库修改流程
1. 修改 \`src/lib/db/pg/schema.pg.ts\`
2. 运行 \`pnpm db:push\` 或 \`pnpm db:generate\`
3. 如需迁移: \`pnpm db:migrate\`

## 常用命令
- \`pnpm dev\` - 启动开发服务器
- \`pnpm build\` - 构建
- \`pnpm db:studio\` - 数据库 GUI
- \`pnpm lint:fix\` - 修复 lint

## 开发约定
- 优先复用 \`components/ui/*\` 中的组件
- 新 API route 添加鉴权: \`auth.api.getSession\`
- 复杂组件抽离到独立文件
- 使用现有 hooks: \`use-categories\`, \`use-templates\`

## 当前功能模块
- Agents (自定义 AI 助手)
- Agent Store (模板商店 - 开发中)
- MCP Servers (工具集成)
- Workflows (工作流)
